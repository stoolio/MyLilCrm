import React from 'react';
import {Router, RouteHandler} from 'react-router';

const App = React.createClass({
  render() {
    return (
      <h1>Hello, world</h1>
    );
  }
});

export default App;
